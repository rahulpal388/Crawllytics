services:
  mongoDB:
    image: mongo:latest
    container_name: seo_mongoDB
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis
    container_name: seo_redis
    ports:
      - "6379:6379"

volumes:
  mongo-data:
